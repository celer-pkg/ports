[package]
  url = "https://gitlab.freedesktop.org/xorg/proto/fixesproto.git"
  ref = "fixesproto-5.0"
  depth = 1

[[build_configs]]
  build_system = "makefiles"
  dev_dependencies = ["macros@1.20.2"]
  patches = ["fix_configure.patch"]
  pre_configure = ["./autogen.sh"]